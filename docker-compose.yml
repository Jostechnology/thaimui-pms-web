version: "3.8"
services:
  app:
    build : .
    platform: linux/amd64
    image: registry.gitlab.com/jostechnology/thaimui-pms-web-dev:thaimui-1.0.7
    container_name: thaimui-pms-web
    ports:
      - "7002:5173"
    environment:
      - VITE_FRONT_API=${VITE_FRONT_API}
      - VITE_GOOGLE_API=${VITE_GOOGLE_API}
      - VITE_DOCUMENT_GENERATOR_URL=${VITE_DOCUMENT_GENERATOR_URL}
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
